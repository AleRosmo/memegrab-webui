import React from "react";

const style = "w-full bg-neutral-500"

const Content = (props) => {
    return (<div className="flex h-screen">
        {/* <SideBar classes={active ? "sidebar active" : "sidebar"} childButtons={buttons} /> */}
    </div>)
}

export default Content